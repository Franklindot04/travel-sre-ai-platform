apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: booking-service
  namespace: observability
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: booking-service
      prometheus: scrape
  namespaceSelector:
    matchNames:
      - platform
  endpoints:
    - port: http
      path: /metrics
      interval: 15s

apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: payment-service
  namespace: observability
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: payment-service
      prometheus: scrape
  namespaceSelector:
    matchNames:
      - platform
  endpoints:
    - port: http
      path: /metrics
      interval: 15s

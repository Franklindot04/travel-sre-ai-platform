apiVersion: v1
kind: Service
metadata:
  name: ai-sre-agent
  namespace: platform
  labels:
    app: ai-sre-agent
    prometheus: scrape
spec:
  selector:
    app: ai-sre-agent
  ports:
    - name: http
      port: 3005
      targetPort: 3005

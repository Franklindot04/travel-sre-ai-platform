apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: platform
  labels:
    app: search-service
    prometheus: scrape
spec:
  selector:
    app: search-service
  ports:
    - name: http
      port: 3001
      targetPort: 3001
